# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Status

This repository is in its initial state with no source code yet. When code is added, update this file with build commands, test commands, and architecture notes.

## Git LFS

This repository is configured for Git LFS. Ensure `git-lfs` is installed before pushing, or the pre-push hook will block the push.
